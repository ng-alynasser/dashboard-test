export const environment = {
	GRAPHQL_URL: 'http://backend.tjarb.com/graphql',
	production: true,
	isMockEnabled: true, // You have to switch this, when your real back-end is done
	authTokenKey: 'authToken'
};
